{
  "name": "servicenow-promin-mcp",
  "version": "1.0.0",
  "description": "MCP server for ServiceNow Process Mining GraphQL API",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "servicenow-promin-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
